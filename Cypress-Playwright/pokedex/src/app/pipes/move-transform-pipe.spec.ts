import { MoveTransformPipe } from './move-transform-pipe';

describe('MoveTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new MoveTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
